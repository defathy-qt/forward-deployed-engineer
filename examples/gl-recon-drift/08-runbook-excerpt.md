# 部署手册（节选）— runbook-iris-v1.0（脱敏）

> 阶段 S7 · 模块 06-deployment-runbook。**工件 ID：runbook-iris-v1.0 + SLO-iris**
> 部署环境：金融专区 · 信创（麒麟/达梦）· 强堡垒机 → 与 10-airgap-deploy 配套；运行方式为离线内网。

## 0. 飞行前
- [x] 架构+ADR-0011 最新；golden-set-iris-v2 达标；红线（过账/改映射不自动）回重进入校验
- [x] 放权等级：L1 长驻 + L2 低风险围栏 = 会计室主任书面；越级动作=无条件冻结
- [x] 维护窗口 2026-09-10 01:00–03:00 已批（专区变更走廊）

## 3.N 部署步骤示例（离线摆渡 + 校验）
```bash
# Command（上传包后，区内执行）
sha256sum -c manifests/sha256.iris-v1.txt && \
  helm/等效导入 镜像+迁移脚本  --fence="low-risk" && \
  python health_check.py --expect runbook-iris-v1
# Expected output
< sha256 OK / bill of materials checksums matched
< {"status":"ok","version":"v1.0","fence":"low-risk","golden":"v2(pass)"}
# If it fails
# 哈希不符→拒载重摆渡；fence 未生效→查明 linchpin；golden 不达标→禁止领取放权
```

## 4.3 可观测确认（2026-09-09 预发实测，专区）
- 指标 12 序列在看板 30s 刷新：归因采纳率 / 定位时长 P95 / 不安全建议计数 / 授权门排队
- 日志 correlation id 串起 文件落地→归因→草稿→授权门→采纳/拒绝
- Trace 3 跳，P95 3.4s（定位单差异）
- **审计**：测试动作 AT-002（虚构"拒绝并说明"）前后值可被审计域全链检索——这是红线级校验
- 告警：14:15 人工制造"映射漂移≥1 列"，108s 触达值班（专区告警链路含强堡垒机会话归因）

## 故障注入（预发，五类全过；含金融特有）
- 数据冲突：映射漂移(GS-017 回放)阻断并告警
- 依赖失败：总账文件晚到 3h → 告警+等待，不做推断
- 重复执行：8 月批重放 → 幂等，不重复起草
- 版本异常：过期 runbook 文档不被采用
- 依据失效：被引凭证在重构后不可解析 → 计依据层失败并转人工

## 7. 回滚（五步，已演练 2026-09-08）
停新 → 回滚 manifest → 映射快照回退（快照 hash 对比）→ flag 恢复基线（L0 或纯人工）→ 健康检查返回 v0.9

## 不可逆动作强制校验（红线落盘，审计可见每处 check）
| check | 强制结果 |
|---|---|
| 草稿→过账是否有「经办+授权复核双签」记录 | 无则拒绝展示提交按钮 |
| subject_mapping 写操作是否带授权门事件 ID | 无则拒绝 |
| 系统是否试图自动推动任何报送 | 任何迹象回滚 |

## SLO-iris（节选）
单笔差异定位 P95 ≤ 15min（基线 40min）；归因采纳率（pilot 口径）≥70% 且 P95 月结时长 ≤2 天；
**不安全动作 = 0**（任意 1 例呼叫并冻结放权）；审计事件 100% 可回溯（红线）。