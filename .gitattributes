# Normalize line endings: store as LF in the repository, check out as LF.
* text=auto eol=lf

# Documentation-first skill kit. The single Python file (scripts/check-align.py)
# is a repository validation tool, not shipped library code, and the corpus is
# Markdown/prose: keep the GitHub language stats from reporting "Python 100%".
scripts/*.py linguist-documentation
*.md linguist-documentation
